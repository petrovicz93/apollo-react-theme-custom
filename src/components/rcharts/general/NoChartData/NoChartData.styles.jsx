import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  nodataContainer: {
    height: '100%',
    display: 'flex',
    alignItems: 'center',
  },
  center: {
    margin: 'auto',
  },
}));
