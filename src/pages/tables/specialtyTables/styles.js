import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) => ({

  selectPlain: {
    backgroundColor: theme.palette.primary.color_white,
    fontSize: '1rem',
    color: '#000',
  },

}));
